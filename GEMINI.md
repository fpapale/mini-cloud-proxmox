Follow the repository bootstrap and scaffold rules defined in `AGENTS.md`.

If `AGENTS.md` is unavailable, apply the same rules:
- preserve existing content
- create only missing files/folders
- scaffold `mini-cloud-proxmox/`
- do not overwrite non-empty files
- do not invent secrets
- do not run destructive or global commands